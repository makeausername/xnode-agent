# API

This repository is currently at Step 1 skeleton stage.

The `pkg/nodeapi` package contains initial DTOs for the future panel contract. No real panel API client behavior is implemented in Step 1.

Planned API responsibilities:

- enroll a node with a panel-issued token
- fetch node and user state from the panel
- report runtime status and traffic counters

These responsibilities are placeholders for later steps.
